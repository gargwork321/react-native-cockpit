// src/constants/index.ts

// Layout Constants
export const FAB_SIZE = 75;
export const PANEL_INITIAL_WIDTH = 300;
export const PANEL_INITIAL_HEIGHT = 400;

// Animation Constants
export const ANIMATION_DURATION = 300;

// Storage Keys (for future use)
export const STORAGE_KEYS = {
  USER_PREFERENCES: 'cockpit_user_prefs',
} as const;

// Default Values
export const DEFAULT_VALUES = {
  LOG_LIMIT: 1000,
} as const;
